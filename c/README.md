The dependency on [Unity](https://github.com/ThrowTheSwitch/Unity) is managed through a git submodule. To clone the submodules in a local repository use

```sh
git submodule update --init --recursive
```
